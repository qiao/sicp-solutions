;; Louis is wrong. The difference between the transfer problem and the
;; exchange problem is that a transfer does not introduce an internal 
;; variable(The ``difference'' variable in exchange). This internal
;; variable gives rise to the problem that inconsistence may occur if
;; two exchange operations are being interleaved.
